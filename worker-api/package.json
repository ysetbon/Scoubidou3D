{
  "name": "mxn-solutions-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + D1 dataset for starred MXN continuation solutions",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute mxn-solutions --remote --file=./schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
